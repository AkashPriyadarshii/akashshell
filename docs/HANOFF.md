# Handoff

- Repo: `C:\Users\saves\Desktop\akashshell`
- Stack: pure Win32 C (`<windows.h>` via `CreateProcess`, `<stdio.h>` for I/O)
- Compiler: MSVC `cl.exe` (or `clang-cl`)
- Next: implement `shell.c` per `AGENTS.md` rules
